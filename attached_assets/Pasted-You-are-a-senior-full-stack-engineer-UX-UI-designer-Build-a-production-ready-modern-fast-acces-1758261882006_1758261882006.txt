You are a senior full-stack engineer + UX/UI designer. Build a production-ready, modern, fast, accessible website for the brand Two Space — a dual concept: Marketing services + Coffee hub. Make it clean, airy, and creative, with tasteful motion.

Tech stack (strict)

Next.js (App Router, TypeScript)

TailwindCSS (+ PostCSS, Autoprefixer)

shadcn/ui (Radix) for primitives

Framer Motion for micro-interactions & transitions

Lenis (optional) for light smooth scroll

next-seo, next/image, @vercel/analytics

Content as local JSON/MDX (no external CMS for now)

Deploy-ready (Vercel defaults)

Global requirements

Design tokens via CSS variables + Tailwind theme:

Colors: --espresso:#2B1E16, --cream:#F5EFE7, --stone:#D9D3CC, --ink:#0F172A, --accent:#3ECF8E, --accentAlt:#7C3AED

Radius 20px, subtle shadow 0 6px 20px rgba(10,10,10,.06)

Typography: Inter for body, General Sans/Satoshi (or similar) for headings.

Accessibility: semantic HTML, focus states, keyboard nav, prefers-reduced-motion respected.

Performance: LCP < 2.0s on Home, CLS ≈ 0, images AVIF/WebP, font-display: swap.

SEO: metadata per page, OpenGraph, schema.org (Organization, LocalBusiness, Product, Event).

Analytics: Vercel Analytics wired.

Motion: 160–240ms UI; 300–600ms section entrances; easing cubic-bezier(0.22,1,0.36,1); disable heavy effects for reduced motion.

Routes & IA

/ (Home)

/marketing (Services landing)

/packages (Pricing & packages comparison)

/cases (Case studies list) + /cases/[slug]

/cafe (Menu & events)

/about

/contact

/blog (stub)

Core components

Navbar (sticky, compact on scroll, CTA “Start a Project”)

WorldSwitch toggle (Marketing/Cafe) that morphs accent color & swaps teasers

HeroTwoWorlds split layout (half Marketing, half Coffee) with masked text reveal, micro-parallax 3–5px on decor, CTA pair

ServiceCard grid (4 cards: Branding, Inițiere, Cercetare, Growth)

PricingTable (3 columns: Start-up / Dezvoltare / Premium; tooltips “What’s included”)

CaseStudyCard (metrics count-up; filters on list page)

MenuItemCard (curved image mask, price, “best seller” badge)

EventCard + mini horizontal calendar (scroll-snap)

Testimonial, LogoWall, CTASection, Footer with legal/GDPR

Content (seed minimal)

Use Romanian copy where shown:

Hero Marketing: “We brew ideas that sell.” Sub: “Branding, lansare, growth. Pachete clare, rezultate măsurabile.”

Hero Cafe: “Coffee for minds at work.” Sub: “Meniu scurt, evenimente, spațiu de idei.”

Packages (from-prices as placeholders):

Start-up: 6.000 RON (Branding Starter)

Launch Kit: 12.000 RON (Branding + Site 5 pagini + Social setup + SEO inițial)

Growth: 6.000–12.000 RON/lună (SMM 2 rețele, Ads, SEO ongoing, email)

Cafe menu: 6–8 items with prices (Espresso 10–12 RON etc.)

2–3 Case studies with fake but plausible metrics (+38% organic, CAC −27%).

Contact: short form (nume, email, buget, obiectiv); CTA to schedule call (dummy link).

File structure (example)
/app
  /(site)
    page.tsx
    layout.tsx
    marketing/page.tsx
    packages/page.tsx
    cases/page.tsx
    cases/[slug]/page.tsx
    cafe/page.tsx
    about/page.tsx
    contact/page.tsx
    blog/page.tsx
/components
  Navbar.tsx, Footer.tsx, WorldSwitch.tsx, HeroTwoWorlds.tsx,
  ServiceCard.tsx, PricingTable.tsx, CaseStudyCard.tsx,
  MenuItemCard.tsx, EventCard.tsx, CTASection.tsx, LogoWall.tsx
/lib
  data.ts (services, packages, menu, cases)
/styles
  globals.css, tokens.css

Styling & tokens

Add a tokens.css with root variables:

:root{
  --bg:#F5EFE7; --ink:#0F172A; --stone:#D9D3CC;
  --espresso:#2B1E16; --accent:#3ECF8E; --accentAlt:#7C3AED;
  --radius:20px; --shadow:0 6px 20px rgba(10,10,10,.06);
}


Tailwind theme should map semantic colors to these tokens.

Motion specs to implement

Section reveal (Framer Motion whileInView, once, amount:0.2, y:12 → 0, opacity 0→1, 0.28s)

Button hover: slight lift, arrow nudge; press scale 0.98

WorldSwitch: layout/motion morph of accent; swap teaser content smoothly

Page transitions: shared layout + cross-fade; keep Navbar persistent

Home layout (sections)

HeroTwoWorlds with two panels + CTAs

Services (4 cards)

Pricing teaser (3 cols)

Case studies carousel (Framer; draggable on mobile)

Cafe teaser (3 menu items + next event)

Testimonials / Logo wall

Final CTA + newsletter

Acceptance criteria

Lighthouse ≥ 90 on Performance/Best Practices/SEO/Accessibility in preview

Valid, keyboard-navigable controls; visible focus rings

No layout shift on LCP; images optimized with next/image

Works on iOS Safari & Android Chrome

WorldSwitch toggle feels instant and delightful; no jank

Tasks (step-by-step)

Scaffold Next.js TS app; add Tailwind, shadcn/ui, Framer Motion, next-seo, @vercel/analytics.

Create tokens, Tailwind theme, globals.

Build Navbar (sticky + compact on scroll) & Footer.

Implement HeroTwoWorlds with motion (masked headline, micro-parallax, CTAs).

Build Services grid (4 ServiceCards with staggered reveal).

Build PricingTable (3 tiers, tooltips, CTA).

Create Cases list (filters) + detail page; seed 2–3 cases.

Build Cafe page (menu grid + events scroll-snap).

About + Contact pages (short form, validation; stub booking link).

Add SEO (next-seo), schema.org JSON-LD (Organization, LocalBusiness, Product, Event).

Integrate Vercel Analytics; prep for Vercel deploy.

Nice to have (if time allows)

Lenis smooth scroll (respect reduced motion)

Skeleton states for async sections

Simple blog index using MDX

Now generate the full project with this spec and seed content, then show a running preview.